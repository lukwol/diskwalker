package com.diskusage.domain.stubs.stubs

import com.diskusage.domain.entities.Arc

object ArcStubs {
    val arc = Arc(
        startAngle = 120.0f,
        sweepAngle = 45.0f,
        depth = 2.0f
    )
}
